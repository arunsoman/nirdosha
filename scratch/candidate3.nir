// Candidate 3: hello + box ownership moved through a channel + role syntax
fn admin_greet(n: i64) -> i64 requires(role: "admin") {
    return n + 1
}

fn sink(c: chan box i64) -> unit {
    let b: box i64 = recv(c)
    print(*b)
}

fn main() {
    print("hello, Nirdosha")

    let c: chan box i64 = chan
    let t: thread unit = spawn sink(c)
    let b: box i64 = box 21
    send(c, b)
    join t
}
